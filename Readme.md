# A paconn sample

## Prerequisites
1. Install paconn cli (https://learn.microsoft.com/en-us/connectors/custom-connectors/paconn-cli#install)
 
## Instructions
1. Clone repo
2. `cd` to repo folder
3. Update `apiDefinition.swagger.json` and `apiProperties.json` with your connector values
4. Run 